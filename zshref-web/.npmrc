# Scoping to this directory (don't adopt the monorepo workspace) is handled by
# the self-rooting pnpm-workspace.yaml — `ignore-workspace` here is not honored
# by `pnpm install`, so don't reintroduce it.
engine-strict=true
auto-install-peers=true
