/**
 * @packageDocumentation
 *
 * Framework-neutral zsh reference tool definitions over `@carlwr/zsh-core`.
 * Exports shared `ToolDef` metadata and pure `(DocCorpus, input) => output`
 * implementations for adapters to register uniformly.
 */

export * from "./src/tool-defs.ts"
export * from "./src/tools/index.ts"
