{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "generic": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Filtered from any tokenization of the query."
    },
    "discriminating_extra": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Filtered additionally when deciding whether a query word is distinctive enough to qualify a record for the exact-word boost."
    }
  },
  "required": [
    "generic",
    "discriminating_extra"
  ],
  "additionalProperties": false,
  "title": "Stopwords",
  "description": "Query stopwords; rank-time."
}
