---
audience: maintainer
read-when: working in packages/zshref-mcp/
---

# AGENTS.md — `@carlwr/zshref-mcp`

MCP server adapting tooldef to the Model Context Protocol.

## Role

Thin adapter over `@carlwr/zsh-core-tooldef`. Static-execution-free posture and import allow-list are enforced from the tooldef package; this package adds only MCP wire-protocol glue.

Tool naming and package conventions live in sibling `DEVELOPMENT.md`.
