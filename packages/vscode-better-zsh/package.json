{
  "name": "better-zsh",
  "displayName": "Better Zsh",
  "description": "Improved zsh shellscript code editing experience",
  "version": "0.0.1",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/carlwr/better-zsh"
  },
  "author": {
    "name": "carlwr",
    "url": "https://github.com/carlwr"
  },
  "publisher": "carlwr",
  "engines": {
    "vscode": "^1.96.0",
    "node": ">=22"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:zsh"
  ],
  "contributes": {
    "languages": [
      {
        "id": "zsh",
        "aliases": [
          "Zsh",
          "zsh"
        ],
        "extensions": [
          ".zsh",
          ".zshrc",
          ".zprofile",
          ".zlogin",
          ".zlogout",
          ".zshenv",
          ".zsh-theme"
        ],
        "filenames": [
          "zshrc",
          "zshenv",
          "zlogin",
          "zprofile",
          "zlogout"
        ],
        "configuration": "./out/language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "zsh",
        "scopeName": "source.shell.zsh",
        "path": "./syntaxes/shell-unix-bash.tmLanguage.json"
      }
    ],
    "snippets": [
      {
        "language": "zsh",
        "path": "./out/snippets.json"
      }
    ],
    "semanticTokenScopes": [
      {
        "language": "zsh",
        "scopes": {
          "function.defaultLibrary": [
            "support.function.builtin.shell"
          ]
        }
      }
    ],
    "configurationDefaults": {
      "[zsh]": {
        "editor.tabSize": 2,
        "editor.insertSpaces": false
      }
    },
    "chatInstructions": [
      {
        "path": "./out/zsh-chat-instructions.md",
        "when": "resourceLangId == zsh"
      }
    ]
  },
  "scripts": {
    "prebuild": "node ../../scripts/build/upstream-ready.mjs ensure",
    "pretypecheck": "node ../../scripts/build/upstream-ready.mjs ensure",
    "pretest": "node ../../scripts/build/upstream-ready.mjs ensure",
    "build": "tsx build.ts",
    "format": "biome check --error-on-warnings --write .",
    "typecheck": "tsc --noEmit",
    "typecheck:vscode-test": "tsc -p tsconfig.vscode-test.json",
    "lint": "biome check --error-on-warnings .",
    "check": "pnpm typecheck && pnpm typecheck:vscode-test && pnpm lint",
    "test": "vitest run",
    "test:integration": "../../scripts/test-integration-act",
    "testINTERACTIVE:electron": "pnpm build && tsc -p tsconfig.vscode-test.json && scripts/testINTERACTIVE-electron",
    "testINTERACTIVE:electron-bundled": "pnpm build && scripts/package-vsix ./better-zsh.vsix && tsc -p tsconfig.vscode-test.json && scripts/testINTERACTIVE-bundled",
    "vsix": "pnpm build && export BZ_SKIP_UPSTREAM=1 && pnpm check && pnpm test && scripts/package-vsix ./better-zsh.vsix",
    "test:pack": "pnpm build && scripts/package-vsix /tmp/better-zsh-pack.vsix && rm /tmp/better-zsh-pack.vsix && echo 'better-zsh pack: OK'",
    "vscode:publish:dry": "pnpm build && export BZ_SKIP_UPSTREAM=1 && pnpm check && pnpm test && node scripts/check-package.mjs && echo '---\n(DRY-RUN; ran staged vsce ls above.)'",
    "vscode:publish": "pnpm build && export BZ_SKIP_UPSTREAM=1 && pnpm check && pnpm test && scripts/package-vsix /tmp/better-zsh-publish.vsix && pnpm exec vsce publish --packagePath /tmp/better-zsh-publish.vsix --no-dependencies",
    "ovsx:publish:dry": "pnpm build && export BZ_SKIP_UPSTREAM=1 && pnpm check && pnpm test && node scripts/check-package.mjs && echo '---\n(DRY-RUN for open-vsx; ran staged vsce ls above.)'",
    "ovsx:publish": "pnpm build && export BZ_SKIP_UPSTREAM=1 && pnpm check && pnpm test && scripts/package-vsix /tmp/better-zsh-publish.vsix && npx -y ovsx publish /tmp/better-zsh-publish.vsix -p $OPEN_VSX_TOKEN"
  },
  "dependencies": {
    "@carlwr/zsh-core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
