{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SentenceFixture",
  "type": "object",
  "properties": {
    "version": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "default-weight": {
      "description": "Weight applied to items that omit `w`; defaults to 1.0 when the\nfixture omits the field too.",
      "type": "number",
      "format": "float",
      "default": 1.0
    },
    "default-target-depth": {
      "description": "Target depth applied to items that omit `d`; defaults to 3.0\nwhen the fixture omits the field too.",
      "type": "number",
      "format": "float",
      "default": 3.0
    },
    "entries": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/SentenceEntry"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "version",
    "entries"
  ],
  "$defs": {
    "SentenceEntry": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string"
        },
        "want": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ExpectedItem"
          }
        },
        "holdout": {
          "description": "Held out from tuning when `true`; absent (the default) ⇒ `Train`.\n`false` is accepted but redundant.",
          "type": "boolean"
        }
      },
      "additionalProperties": false,
      "required": [
        "query",
        "want"
      ]
    },
    "ExpectedItem": {
      "description": "One scored item within an entry's `want` set. Each item is ranked on\nits OWN rank and contributes one vote; an N-item entry carries N votes.\n`d`/`w` override the fixture-level defaults when present; [`load`]\nresolves them to concrete values, so callers should read them via\n[`ExpectedItem::effective_depth`] / [`ExpectedItem::effective_weight`].",
      "type": "object",
      "properties": {
        "cat": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "d": {
          "type": [
            "number",
            "null"
          ],
          "format": "float"
        },
        "w": {
          "type": [
            "number",
            "null"
          ],
          "format": "float"
        }
      },
      "additionalProperties": false,
      "required": [
        "cat",
        "id"
      ]
    }
  }
}
