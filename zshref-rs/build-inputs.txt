# Build fingerprint inputs. Paths are relative to zshref-rs.
file build-inputs.txt
file build.rs
file Cargo.toml
file Cargo.lock
# Extensions are data both implementations read, not a list duplicated in each.
# `json` is deliberately absent: `src/nlp/rules/schema/*.schema.json` is
# regenerated by `UPDATE_SCHEMAS=1`, so hashing it would let a test run
# invalidate the binary that test just ran against.
src-tree src rs yaml
json-data
